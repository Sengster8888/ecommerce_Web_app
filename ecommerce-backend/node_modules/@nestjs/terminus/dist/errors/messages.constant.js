/**
 * @internal
 */
export const STORAGE_EXCEEDED = (keyword) => `Used ${keyword} exceeded the set threshold`;
/**
 * @internal
 */
export const DATABASE_NOT_CONNECTED = `Not connected to database`;
