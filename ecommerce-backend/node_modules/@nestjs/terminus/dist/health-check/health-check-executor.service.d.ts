import { type BeforeApplicationShutdown, type LoggerService } from '@nestjs/common';
import { type HealthCheckResult } from './health-check-result.interface.js';
import { type InferHealthIndicatorResults, type HealthIndicatorFunction } from '../health-indicator/index.js';
import { type TerminusModuleOptions } from '../terminus-options.interface.js';
/**
 * This class is responsible for executing the health indicators and returning the result.
 *
 * @internal
 */
export declare class HealthCheckExecutor implements BeforeApplicationShutdown {
    private readonly logger;
    private readonly options;
    private isShuttingDown;
    constructor(logger: LoggerService, options: TerminusModuleOptions);
    /**
     * Executes the given health indicators.
     * Implementation for v6 compatibility.
     *
     * @throws {Error} Any error thrown by a health indicator
     *
     * @returns the result of given health indicators
     * @param healthIndicators The health indicators which should get executed
     */
    execute<const TFns extends HealthIndicatorFunction[]>(healthIndicators: TFns): Promise<HealthCheckResult<InferHealthIndicatorResults<TFns>>>;
    /**
     * @internal
     */
    beforeApplicationShutdown(signal?: string): Promise<void>;
    private executeHealthIndicators;
    private getSummary;
    private getResult;
}
