import { LoggerService } from '@nestjs/common';
import { type HealthIndicatorFunction } from '../health-indicator/index.js';
import { ErrorLogger } from './error-logger/error-logger.interface.js';
import { HealthCheckExecutor } from './health-check-executor.service.js';
/**
 * Handles Health Checks which can be used in
 * Controllers.
 */
export declare class HealthCheckService {
    private readonly healthCheckExecutor;
    private readonly errorLogger;
    private readonly logger;
    constructor(healthCheckExecutor: HealthCheckExecutor, errorLogger: ErrorLogger, logger: LoggerService);
    /**
     * Checks the given health indicators
     *
     * ```typescript
     *
     * healthCheckService.check([
     *   () => this.http.pingCheck('google', 'https://google.com'),
     * ]);
     *
     *
     * ```
     * @param healthIndicators The health indicators which should be checked
     */
    check<const TFns extends HealthIndicatorFunction[]>(healthIndicators: TFns): Promise<import("./health-check-result.interface.js").HealthCheckResult<import("../health-indicator/health-indicator-result.interface.js").InferHealthIndicatorResults<TFns, []>>>;
}
